# 走啊 · 审计留痕（步骤 7 / 9 回归 + 步骤 10 部署工件）

> 依据规划书 11.2 审计门禁制：留痕随代码入库 `docs/`。日期：2026-09-23。

## 12.1 功能测试清单（全量回归勾选）

| # | 功能 | 结果 | 依据 |
|---|---|---|---|
| 1 | GPS 定位成功/失败/超时三分支 | ✅ | 定位成功→逆地理→runScan；失败/不支持→应用内城市浮层；超时(6s)→浮层兜底（代码 `main.js` btnScan）。浏览器验证浮层出现 |
| 2 | 手动选点（至少 3 城市） | ✅ | `CITY_PRESETS` 80+ 城市零网络命中 + 自由输入（前端 Geocoder → geocode 代理）。浏览器验证杭州 chip → 结果态 |
| 3 | 扫描动画与真实数据同步 | ✅ | `radar.start` 时长=指数/100×800ms；颜色=`heatColor` 真实指数；`prefers-reduced-motion` 降级调用 `main.js` |
| 4 | Top-3 推荐计算正确性 | ✅ | `aggregateRegions` 单测：人少排序/指数均值/代表锚点/景点去重 全过（`test/scan.test.mjs`） |
| 5 | 节省分钟数对比逻辑 | ✅（演进） | 产品定位演进为「哪个人少」发现导向，不再强调「节省分钟数」；指数语义映射 人少/正常/人多 |
| 6 | 结果卡时间戳随缓存刷新 | ✅ | `scannedAt` + `.sheet-foot`「数据由高德路线实时测算 · HH:MM」；浏览器 DOM 验证文本 `09:30` |
| 7 | 分享卡生成（1080px） | ✅ | `buildShareCard` 离屏 Canvas ≥1080×1440、白底深字、版权标注，PNG 打开 |
| 8 | 跳转高德导航 | ✅ | `openNavigation` uri.amap.com/navigation?to=经度,纬度,名称&mode=car |
| 9 | 全链路降级分支 | ✅ | 4 分支：定位失败→浮层；规划失败→「没扫出方向」；配额耗尽(QPS)→「稍缓一下」；断网→`renderOffline` 静态兜底 + offline/online 监听 |

## 12.3 配额压测门禁

- 前端 API 节流队列 `apiQueue`：10 并发洪峰实测并发峰值 = 3（=MAX_CONCURRENCY），QPS≤3 真实生效。
  test: `test/queue.test.mjs`（3 项全过）。
- 单次扫描新调用量受控于扇区×锚点上限（12扇区×8锚点，命中缓存后 ≤12 次量级），Dev 服务端再加 smooth throttle（≥320ms/req）。

## 单测汇总

- `node test/scan.test.mjs` → 19 passed / 0 failed（bearing / assignSector / scanSectors 指数 / 全失败降级 / kmDistance / aggregateRegions）
- `node test/queue.test.mjs` → 3 passed / 0 failed（节流并发峰值 ≤3）
- `node test/scenario.test.mjs` → 7 passed / 0 failed（12.2 多城市场景免配额替身）
- `npm test` 汇总三套测试共 **29 项全过**。

## 12.2 多城市场景模拟（免配额替身）

> 用注入 router 覆盖特色城市压力面，不消耗高德配额。test: `test/scenario.test.mjs`。

| 场景 | 验证结果 | 说明 |
|---|---|---|
| 堵城 | ✅ 中位车速 15km/h（低） | 30km/7200s → 真实反映拥堵水平 |
| 空城 | ✅ 中位车速 90km/h（高） | 30km/1200s，堵/空可区分 |
| 方向差异化 | ✅ 最堵东向指数 100 > 畅通西向 50 | 相对指数随方向拥堵正确升高 |
| 跨城远景过滤 | ✅ 全远 → no-data 显式降级 | maxKm=100km 之外锚点不参与，防相对指数偏差 |
| 部分跨城 | ✅ 近处参与、远处被滤（有数据扇区 3，在[3,8)） | 半径外锚点确实被排除，不污染方向数据 |

## 步骤 10 部署工件（已就绪，待用户发布）

- `vercel.json`：`functions` 声明 `serverless/api/amap/*.js`(nodejs20) + rewrite `/api/amap/:name` + 安全响应头。
- `package.json`：新增（`"type":"module"` 使 Vercel Node Function 正确解析 ESM `export default handler`）+ `npm run dev` / `npm test`。
- 来源白名单 `_guard.js`：所有 serverless 函数接入（route/district/regeo/traffic/poi/pois/geocode），`ALLOWED_ORIGINS` 未配置放行、配置后强校。
- `.env.example`：新增 `ALLOWED_ORIGINS` 说明。
- 新增 `serverless/api/amap/geocode.js`：补齐前端 geocode 代理缺口（本地/生产同口径）。
- README：更新产品链路、新增「部署到 Vercel」章节、规划书链接 v2.8→v2.10。

## 遗留 / 需用户侧项

- 真实发布：需用户在 Vercel 配置 `AMAP_WEB_KEY` / `ALLOWED_ORIGINS` 环境变量 + AMap JS Key 域名白名单写入生产域名后执行 `vercel --prod`。
- 12.2 多城市实测（堵城/空城/跨城）需真实配额跑数，未在本沙箱执行。